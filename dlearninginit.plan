initially:
      -subtema_ensenado(S).
      -sabe_subtema(S).
      -esta_en_asesorias.
      necesita_asesorias(primero).
      %-sabe_subtema(primero).
      %-sabe_subtema(segundo).

%securePlan.
noConcurrency.
goal: sabe_subtema(S)? (2)
